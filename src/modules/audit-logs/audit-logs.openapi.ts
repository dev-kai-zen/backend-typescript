/**
 * OpenAPI fragments for `audit-logs.routes.ts`.
 * Consumed by swagger-jsdoc via `config/swagger-config.ts` (`apis` glob); not imported at runtime.
 */

/**
 * @openapi
 * /audit-logs:
 *   get:
 *     tags: [Audit logs]
 *     summary: List audit logs
 *     responses:
 *       200:
 *         description: OK
 */

export {};
